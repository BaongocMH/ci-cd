name: CI/CD

on:
   push:
     branches: ["main"]

jobs:
  thuc_hanh_doc_file:
    runs-on: ubuntu-latest

    steps: 
      - name: Bước 1  - Xin chào
        run: echo "Hello Hutech!"

      - name: Bước 2 - Tải code
        run: actions/checkout@6

      - name: Bước 3 - Đọc file
        run: cat README.md
        
